Построение:
  Выполните последовательно
    cd src
    qmake CONFIG+="release" && make
Установка:
  Скопируйте исполняемый файл в один каталог с файлом перевода
(*.qm).

---------------------------------------------------------------------

BUILD:
  Type this in command line
    cd src
    qmake CONFIG+="release" && make
INSTALL:
  Copy executable file (Rong*) and translation file (*.qm) to any 
folder.

